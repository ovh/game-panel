# Gamepanel SFTP users (no shell)
Match User gp_s*
  ForceCommand internal-sftp -d /data
  PasswordAuthentication yes
  PubkeyAuthentication yes
  X11Forwarding no
  AllowTcpForwarding no
  PermitTunnel no
  PermitTTY no

Match all
